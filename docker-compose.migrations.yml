version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=BankApp_Identity
      - BankApp_DB=BankApp_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
